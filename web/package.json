{
  "name": "edge-detection-viewer",
  "version": "1.0.0",
  "description": "Web viewer for real-time edge detection",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "serve": "npx http-server . -p 3000"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "http-server": "^14.0.0"
  }
}